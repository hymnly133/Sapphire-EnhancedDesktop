#include "swebviewer.h"
// #include"qweb

SWebViewer::SWebViewer(SLayout *dis, int sizex, int sizey): SUnit(dis, sizex, sizey)
{
    // QWebEngineView *webView = new QWebEngineView(this);
    // webView->load(QUrl("https://www.example.com"));
}
